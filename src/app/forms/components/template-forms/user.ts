export class User {
    // constructor(public firstName: string, public lastName: string, public birthDate : any) 
    constructor(public firstName: string, public lastName: string, public email:string,public phone:string){
    }
}
